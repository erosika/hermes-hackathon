# Supabase client (real auth — magic link / OTP). copy to .env.local and fill.
# Dashboard → Project Settings → API. these two are safe to expose to the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
